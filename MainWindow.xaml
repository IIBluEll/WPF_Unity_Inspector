<Window x:Class="WPF_UnityInspector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:WPF_UnityInspector.Views"
        Title="Unity Project Inspector"
        Width="1000"
        Height="700">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="20,14"
                Background="#FAFAFA"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="IsEnabled"
                            Value="True" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}"
                                     Value="True">
                            <Setter Property="IsEnabled"
                                    Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Unity Project Inspector"
                           FontSize="22"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />

                <StackPanel Grid.Column="1"
                            Margin="32,0"
                            VerticalAlignment="Center">

                    <TextBlock Text="{Binding ProjectName}"
                               FontWeight="SemiBold" />

                    <TextBlock Text="{Binding ProjectPath}"
                               Foreground="Gray"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <Button Content="Select Unity Project"
                            Width="160"
                            Height="34"
                            Click="OnSelectProjectClicked"
                            Margin="0,0,8,0" />

                    <Button Content="Refresh"
                            Width="90"
                            Height="34"
                            Click="OnRefreshProjectClicked" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1"
                    TabStripPlacement="Left"
                    BorderThickness="0"
                    Background="#F4F6F8">

            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Width"
                            Value="200" />
                    <Setter Property="Height"
                            Value="46" />
                    <Setter Property="Foreground"
                            Value="#B8BDC7" />
                    <Setter Property="FontSize"
                            Value="14" />
                    <Setter Property="FontWeight"
                            Value="SemiBold" />
                    <Setter Property="Cursor"
                            Value="Hand" />
                    <Setter Property="FocusVisualStyle"
                            Value="{x:Null}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="ItemBorder"
                                        Background="Transparent">

                                    <Grid>
                                        <Border x:Name="SelectionIndicator"
                                                Width="3"
                                                HorizontalAlignment="Left"
                                                Background="#4C8DFF"
                                                Visibility="Collapsed" />

                                        <ContentPresenter ContentSource="Header"
                                                          Margin="18,0,12,0"
                                                          VerticalAlignment="Center"
                                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                                    </Grid>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter TargetName="ItemBorder"
                                                Property="Background"
                                                Value="#262D37" />
                                    </Trigger>

                                    <Trigger Property="IsSelected"
                                             Value="True">
                                        <Setter TargetName="ItemBorder"
                                                Property="Background"
                                                Value="#2C3542" />
                                        <Setter TargetName="SelectionIndicator"
                                                Property="Visibility"
                                                Value="Visible" />
                                        <Setter Property="Foreground"
                                                Value="White" />
                                    </Trigger>

                                    <Trigger Property="IsEnabled"
                                             Value="False">
                                        <Setter Property="Opacity"
                                                Value="0.55" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabControl.Style>
                <Style TargetType="TabControl">
                    <Setter Property="IsEnabled"
                            Value="True" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}"
                                     Value="True">
                            <Setter Property="IsEnabled"
                                    Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Style>

            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="#1E232B">

                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top"
                                           Text="NAVIGATION"
                                           Margin="18,20,18,12"
                                           Foreground="#77808D"
                                           FontSize="11"
                                           FontWeight="Bold" />

                                <TabPanel IsItemsHost="True"
                                          KeyboardNavigation.TabNavigation="Local" />
                            </DockPanel>
                        </Border>

                        <Border Grid.Column="1"
                                Background="{TemplateBinding Background}">

                            <ContentPresenter Content="{TemplateBinding SelectedContent}"
                                              ContentTemplate="{TemplateBinding SelectedContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding SelectedContentTemplateSelector}"
                                              ContentStringFormat="{TemplateBinding SelectedContentStringFormat}" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
            <TabItem Header="Dashboard">
                <views:DashboardView />
            </TabItem>

            <TabItem Header="Build Assets">
                <views:BuildAssetsView />
            </TabItem>

            <TabItem Header="Messages">
                <views:MessagesView />
            </TabItem>
        </TabControl>

        <Grid Grid.RowSpan="2"
              Panel.ZIndex="100"
              Background="#E6111318"
              Cursor="Wait">

            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility"
                            Value="Collapsed" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}"
                                     Value="True">
                            <Setter Property="Visibility"
                                    Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Border Width="420"
                    Padding="32"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="#252A33"
                    CornerRadius="10">

                <StackPanel>
                    <TextBlock Text="프로젝트 분석 중"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="White"
                               HorizontalAlignment="Center" />

                    <TextBlock Text="{Binding LoadingMessage}"
                               Margin="0,10,0,24"
                               Foreground="#B8BDC7"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />

                    <ProgressBar Height="5"
                                 IsIndeterminate="True" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
